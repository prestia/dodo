<h1 style="text-transform:capitalize"><%= @path %></h1>
<ul style="list-style:none; margin:0px; padding:0px;">
  <% if archives.length > 0 %>
    <% for entry in archives %>
      <li>
        <a href="<%= entry.path %>"><%= entry.title %></a>
      </li>
    <% end %>
  <% end %>
</ul>

