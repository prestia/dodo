<section id="articles">
  <% articles.select {|a| a[:date] <= Date.today}[0...10].each do |article| %>
    <article class="post">
      <header>
        <h1><a href="<%= article.path %>"><%= article.title %></a></h1>
        <span class="date"><%= article.date %></span>
      </header>

      <section class="content">
        <% if @config[:fulltext] == false %><%= article.summary %>
	    <% else %><%= article.body %>
	    <% end %>
      </section>
      <% if @config[:fulltext] == false %><div class="more"><a href="<%= article.path %>">view article &raquo;</a></div><% end %>
    </article>
  <% end %>
</section>

<!--<section id="archives">
  <%= archives[3..-1] %>
</section>-->